{
  "name": "inkstone-v2-site",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:hexo": "node scripts/migrate-hexo.mjs",
    "build": "npm install --no-audit --no-fund --ignore-scripts --package-lock=false && node scripts/build-v2.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "markdown-it": "15.0.0",
    "markdown-it-task-lists": "2.1.1",
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
